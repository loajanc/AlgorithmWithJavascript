
// 실행안됨 

// ?? node js환경이 아니라서 그런가?

let fs = require('fs');
let input = fs.readFileSync('/dev/stdin').toString().split(' ');

let nunm = Number(input);

for(let i = 1; i <= Number; i++){
    console.log(i);
}